#ifndef MULTITEST_H
#define MULTITEST_H
#include <stdio.h>
#include <stdlib.h>
#define dummysearch(x, y, z, q) search(x, y, z, q)

//might need to check into some of this stuff
int search(int array[], int size, int breaksize, int target);

#endif